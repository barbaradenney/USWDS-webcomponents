# pnpm configuration for monorepo
# Use hoisted node_modules for Vite compatibility
node-linker=hoisted

# Prevent automatic installation on usages
auto-install-peers=false

# Strict peer dependencies
strict-peer-dependencies=false
